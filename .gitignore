# Editor
.vscode/

# Backup area (not synced)
_backup

# Machine-specific settings (git-ignored) — merged with the home/settings.json
# baseline and synced to ~/.claude/settings.json. Never commit.
settings.override.json
